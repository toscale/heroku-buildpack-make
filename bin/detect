#!/usr/bin/env bash
# bin/detect <build-dir>
BUILD_DIR=$1
cd $BUILD_DIR

echo "-----> Finding Makefile"

if [ -f "Makefile" ]
then
  echo "-----> Makefile found"
else
  echo "-----> No Makefile found. Compilation cannot continue."
  exit 1;
fi
